export function renderProfile(t,{e,catName,tools}){
 const li=items=>'<ul>'+items.map(v=>`<li>${e(v)}</li>`).join('')+'</ul>';
 const section=(id,title,items)=>items?.length?`<section class="profile-section" id="${id}"><h2>${title}</h2>${li(items)}</section>`:'';
 const related=tools.filter(r=>r.slug!==t.slug&&r.status!=='draft'&&r.primaryCategory===t.primaryCategory).slice(0,4);
 const source=t.contentSource;
 const facts=[['Vendor',t.vendor],['Free plan',t.pricing.freePlan===true?'Listed in source — see restrictions':t.pricing.freePlan===false?'No ongoing free AI plan listed':'Limited or unclear — see details'],['Free trial',t.pricing.freeTrial===true?'Listed in source':t.pricing.freeTrial===false?'No separate trial listed':'Check current availability'],['Billing',t.pricing.billingPeriod||'Not specified'],['Currency',t.pricing.currency||'Not specified in source']];
 const body=`<div class="page-head"><span class="eyebrow">${e(catName(t.primaryCategory))} · ${e(t.vendor)}</span><h1>${e(t.name)}</h1><p>${e(t.shortDescription)}</p><div class="profile-actions"><a class="button" href="${e(t.officialUrl)}">Visit official website ↗</a><a class="text-link" href="#pricing">Explore pricing & limits</a></div></div><div class="notice source-notice">Research summary updated <time datetime="${t.contentUpdated}">${t.contentUpdated}</time>. Product details and price estimates come from supplied research and may change. Confirm important details with the official sources below.</div><div class="profile-nav" aria-label="On this page"><a href="#overview">Overview</a><a href="#features">Features</a><a href="#use-cases">Use cases</a><a href="#pricing">Pricing</a><a href="#tradeoffs">Tradeoffs</a><a href="#connections">Compatibility</a><a href="#faq">FAQ</a></div><div class="tool-layout"><article class="prose"><section class="profile-section" id="overview"><h2>What is ${e(t.name)}?</h2><p>${e(t.description)}</p><div class="best-for"><span class="eyebrow">Best for</span><p>${e(t.bestFor)}</p></div></section>${section('features','Main features',t.features)}${section('use-cases','Practical use cases',t.useCases)}${section('audience','Who is it for?',t.audiences)}<section class="profile-section" id="pricing"><h2>Pricing, free access & limits</h2><p>${e(t.pricing.modelDetails)}</p><h3>Plans described in the source</h3><p>${e(t.pricing.notes)}</p><div class="pricing-notes"><div><h3>Free plan</h3><p>${e(t.pricing.freePlanNotes)}</p></div><div><h3>Free trial</h3><p>${e(t.pricing.freeTrialNotes)}</p></div></div><h3>Usage limits to consider</h3><p>${e(t.pricing.usageLimits)}</p><a class="text-link" href="${e(t.pricing.url)}">Confirm current plans and pricing ↗</a></section><section class="profile-section" id="tradeoffs"><h2>Strengths & tradeoffs</h2><p class="muted-small">These points summarize the supplied research, rather than hands-on test results.</p><h3>Strengths noted in the source</h3>${li(t.advantages)}<h3>Limitations to consider</h3>${li(t.limitations)}</section><section class="profile-section" id="connections"><h2>Platforms, languages & integrations</h2><h3>Platforms</h3>${li(t.platforms)}<h3>Languages</h3>${li(t.languages)}<h3>API access</h3><p>${e(t.api.notes)}</p><h3>Integrations and exports</h3>${li(t.integrations)}</section><section class="profile-section"><h2>Evaluation notes</h2><p>${e(t.testing.notes)}</p></section>${related.length?`<section class="profile-section"><h2>Explore similar tools</h2><p>Other tools in ${e(catName(t.primaryCategory))}. Compare their specific features and limits for your task.</p><div class="related-tools">${related.map(r=>`<a href="/tools/${r.slug}/"><strong>${e(r.name)}</strong><span>${e(r.bestFor)}</span></a>`).join('')}</div></section>`:''}<section class="profile-section" id="faq"><h2>Frequently asked questions</h2>${t.faqs.map(f=>`<details><summary>${e(f.question)}</summary><p>${e(f.answer)}</p></details>`).join('')}</section><section class="profile-section" id="sources"><h2>Sources & editorial notes</h2><p>Based on the directory editor’s supplied research. ${source.sourceVerificationDate?`The source marks this row as checked on ${e(source.sourceVerificationDate)}.`:'The source does not provide a verification date for this row.'} Importing a record does not independently verify every product claim.</p><ul>${t.sources.map(s=>`<li><a href="${e(s.url)}">${e(s.label)}</a></li>`).join('')}</ul><details><summary>Additional references named in the research</summary><p>${e(source.evidence)}</p></details></section></article><aside class="facts"><span class="eyebrow">At a glance</span><dl>${facts.map(([k,v])=>`<dt>${k}</dt><dd>${e(v)}</dd>`).join('')}<dt>Categories</dt><dd>${t.categories.map(c=>`<a href="/categories/${c}/">${e(catName(c))}</a>`).join('<br>')}</dd><dt>Specialties</dt><dd>${e(t.subcategories.join(' · '))}</dd></dl><p><a class="text-link" href="#sources">Sources & editorial notes ↓</a></p></aside></div>`;
 const schema={'@type':'SoftwareApplication',name:t.name,description:t.shortDescription,url:t.officialUrl,applicationCategory:catName(t.primaryCategory),publisher:{'@type':'Organization',name:t.vendor},operatingSystem:t.platforms.join('; '),featureList:t.features,dateModified:t.contentUpdated};
 return {body,extra:[schema]};
}
